{
  "name": "iava-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf _site",
    "build": "eleventy",
    "serve": "eleventy --serve",
    "dev": "npm run clean && npm run serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "rimraf": "^5.0.10"
  }
}
